{
  "name": "moneygun",
  "private": true,
  "version": "0.1.0",
  "description": "Signal Guild private AI investment-company operating system",
  "workspaces": [
    "apps/web"
  ],
  "scripts": {
    "dev": "npm run dev --workspace @moneygun/web",
    "api": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/start-api.ps1",
    "build": "npm run build --workspace @moneygun/web",
    "typecheck": "npm run typecheck --workspace @moneygun/web",
    "preview": "npm run preview --workspace @moneygun/web"
  },
  "engines": {
    "node": ">=24"
  }
}
